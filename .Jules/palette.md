# Palette's Journal

## 2024-05-23 - Gradio Info Tooltips
**Learning:** Gradio's `info` parameter is essential for explaining technical model choices (e.g. "qwen3" vs "minilm") directly in the context of the control, reducing cognitive load for non-technical users.
**Action:** Always check `gr.Dropdown` and `gr.Slider` for technical parameters and add `info` explanations.
